import SwiftUI

extension Color {
    // MARK: - Backgrounds
    static let bgPrimary = Color("bgPrimary")
    static let bgCard = Color("bgCard")
    static let bgSurface = Color("bgSurface")
    static let bgInput = Color("bgInput")
    static let bgEmoji = Color("bgEmoji")

    // MARK: - Text
    static let textPrimary = Color("textPrimary")
    static let textSecondary = Color("textSecondary")
    static let textMuted = Color("textMuted")
    static let textLabel = Color("textLabel")

    // MARK: - Accents
    static let accentPrimary = Color("accentPrimary")
    static let accentSuccess = Color("accentSuccess")
    static let accentDanger = Color("accentDanger")
    static let accentWarning = Color("accentWarning")
    static let accentViolet = Color("accentViolet")
    static let accentSettled = Color("accentSettled")

    // MARK: - Borders
    static let borderPrimary = Color("borderPrimary")
    static let borderHover = Color("borderHover")
}
